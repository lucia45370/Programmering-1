from operator import truediv


Username= "luciayety3"
password= "lucia122"
logged_in = True
tries=0
pass

while True :
 print ('did you know Jefferey Dahmer was a dull kid and that made his father sent him to the army.')
else :
    print ('Jefferey Dahmer is not real.')
pass



accounts = {}
logged_in = False

while True:
    menyval = input(
        "1. Skapa konto\n"
        "2. Logga in\n"
        "3. Läs en rolig historia\n"
        "4. Logga ut\n"
        "5. Avsluta program\n"
    )

    if menyval == "1":
        Username= "luciayety3"
        password= "lucia122"
        logged_in = True
        tries=0
        # TODO Skapa ett konto genom att lägga till ett key-value par i accounts
        # username = key, password = value
        # Bonus: Kolla först så att användaren inte redan finns
        pass

    elif menyval == "2":
        while tries <3:
            user1 = input('username:')
            input("password:")
    print (logged_in)
    logged_in = true
if user1 == username and password:
        print("logged_in")
        logged_in = true
else:
    print ("wron username or pw/n")
    tries += 1
while True :
 print ('did you know Jefferey Dahmer was a dull kid and that made his father sent him to the army.')
else :
    print ('Jefferey Dahmer is not real.')
elif menyval == "4":
    
    elif menyval == "5":
        break
