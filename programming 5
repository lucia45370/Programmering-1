# Travelbag
# Skelettkod till uppgiften

# skapa en lista 
resväskan = ['toothbrush','sanitary pads','pygamas','trousers','tops','shoes','cross bags','towels','skincare','jackets']
name=input ('vad heter du?')
print ('valkommen', name, 'till mitt program.jag ska resa till Italy och jag behover hjälp att plocka upp mina saker.')
while True:
   menyval = input("1. Kolla i resväskan\n"
                   "2. Lägg sak i resväskan\n"
                   "3. Ta bort sak i resväskan\n"
                   "4. Avsluta program")

   if menyval == "1":
    print (*resväskan)
   elif menyval == "2":
        name=input("vad vill du l'gga till")
        resväskan.append(name)
        print(*resväskan)
   elif menyval =="3":
        name=input()
        resväskan.remove(name)
        print (*resväskan)

   elif menyval =="4":
            break